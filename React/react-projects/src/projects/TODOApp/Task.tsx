// one task this will render one task at a time.
export default function Task() {
  // when the user clicks the add button.. add another task to to array ?
  return (
    <div>
      <input type="checkbox"></input>
      <input type="text"></input>
      <button>Edit</button>
      <button>Delete</button>
    </div>
  );
}
